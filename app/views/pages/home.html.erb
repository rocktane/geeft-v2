<h1 class="system">Hello World</h1>
<h1 class="oto">Hello World</h1>
<h1 class="abz">Hello World</h1>
<% if @user == "guest" %>
  <p><%= @user %></p>
<% else %>
  <%= "email : " + @user.email if @user.email %>
  <%= "username : " + @user.username if @user.username%>
<% end %>

<br>
<%= link_to 'dashboard', dashboard_path %>
<%= link_to 'generate list', new_gift_path %>
